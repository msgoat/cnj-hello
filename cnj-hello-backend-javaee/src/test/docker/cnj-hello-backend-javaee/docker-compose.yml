# docker-compose.yml
# ----------------------------------------------------------------------------
# Composes Docker containers and volumes to a runnable Java EE application
# ----------------------------------------------------------------------------
#
---
version: '3'

services:

  payara:
    image: docker.at.automotive.msg.team/cloudtrain/cnj-hello-backend-javaee:latest
    ports:
      - "18080:8080"
      - "4848:4848"
